<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} - Tellngrow Counselor</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes fadeInDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    @keyframes scaleIn {
      from {
        opacity: 0;
        transform: scale(0.95);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }
    
    .animate-fadeInDown {
      animation: fadeInDown 0.6s ease-out;
    }
    .animate-fadeInUp {
      animation: fadeInUp 0.6s ease-out;
    }
    .animate-scaleIn {
      animation: scaleIn 0.6s ease-out;
    }
    .form-group {
      animation: fadeInUp 0.6s ease-out backwards;
    }
    .form-group:nth-child(1) { animation-delay: 0.1s; }
    .form-group:nth-child(2) { animation-delay: 0.2s; }
    .form-group:nth-child(3) { animation-delay: 0.3s; }
    .form-group:nth-child(4) { animation-delay: 0.4s; }
    .form-group:nth-child(5) { animation-delay: 0.5s; }
    .form-group:nth-child(6) { animation-delay: 0.6s; }
    
    .form-input, .form-select, .form-textarea {
      border-color: #e5e7eb;
      transition: all 0.3s ease;
    }
    .form-input:focus, .form-select:focus, .form-textarea:focus {
      border-color: #16a34a;
      box-shadow: 0 0 0 3px rgba(22, 163, 74, 0.1);
    }
    
    .btn-primary {
      background: linear-gradient(135deg, #16a34a 0%, #15803d 100%);
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(22, 163, 74, 0.3);
    }
  </style>
</head>
<body class="bg-white min-h-screen">
  <!-- Navigation -->
  <nav class="bg-white border-b-2 border-green-100 shadow-sm sticky top-0 z-50">
    <div class="container mx-auto flex justify-between items-center px-6 py-4">
      <div class="flex items-center space-x-4">
        <a href="/counselor/dashboard" class="text-2xl font-bold text-green-600 hover:text-green-700 transition">🌱 Tellngrow Counselor</a>
      </div>
      <div class="space-x-6 flex items-center text-gray-700">
        <a href="/counselor/dashboard" class="hover:text-green-600 transition font-medium">Dashboard</a>
        <a href="/counselor/materials" class="hover:text-green-600 transition font-medium">Materials</a>
        <span class="text-gray-500">|</span>
        <span>👨‍💼 <span class="font-semibold">{{user.name}}</span></span>
        <a href="/logout" class="text-red-600 hover:text-red-700 transition font-medium">Logout</a>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container mx-auto px-6 py-12 max-w-4xl">
    <!-- Header -->
    <div class="mb-8 animate-fadeInDown">
      <h1 class="text-4xl font-bold text-gray-900 mb-2">Edit Material</h1>
      <p class="text-lg text-gray-600">Update your educational content and settings</p>
      <div class="w-24 h-1 bg-gradient-to-r from-green-500 to-emerald-500 rounded-full mt-4"></div>
    </div>

    <!-- Form Container -->
    <div class="bg-white border-2 border-green-100 rounded-2xl p-8 shadow-lg animate-scaleIn">
      <form action="/counselor/materials/update/{{material.id}}" method="POST" onsubmit="handleFormSubmit(event)">
        <!-- Title -->
        <div class="form-group mb-8">
          <label class="block text-gray-800 font-bold mb-3 text-lg">📌 Title <span class="text-red-500">*</span></label>
          <input type="text" name="title" required value="{{material.title}}"
                 class="form-input w-full px-4 py-3 border-2 rounded-lg focus:outline-none"
                 placeholder="Material title">
        </div>

        <!-- Category -->
        <div class="form-group mb-8">
          <label class="block text-gray-800 font-bold mb-3 text-lg">🏷️ Category <span class="text-red-500">*</span></label>
          <select name="category" required
                  class="form-select w-full px-4 py-3 border-2 rounded-lg focus:outline-none bg-white">
            <option value="Mental Health" {{#if (eq material.category 'Mental Health')}}selected{{/if}}>🧠 Mental Health</option>
            <option value="Stress Management" {{#if (eq material.category 'Stress Management')}}selected{{/if}}>😌 Stress Management</option>
            <option value="Anxiety" {{#if (eq material.category 'Anxiety')}}selected{{/if}}>💙 Anxiety</option>
            <option value="Wellness" {{#if (eq material.category 'Wellness')}}selected{{/if}}>🌟 Wellness</option>
            <option value="Self-Care" {{#if (eq material.category 'Self-Care')}}selected{{/if}}>💆 Self-Care</option>
            <option value="Mindfulness" {{#if (eq material.category 'Mindfulness')}}selected{{/if}}>🧘 Mindfulness</option>
            <option value="Fitness" {{#if (eq material.category 'Fitness')}}selected{{/if}}>💪 Fitness</option>
            <option value="Other" {{#if (eq material.category 'Other')}}selected{{/if}}>📚 Other</option>
          </select>
        </div>

        <!-- Excerpt -->
        <div class="form-group mb-8">
          <label class="block text-gray-800 font-bold mb-3 text-lg">✍️ Short Description <span class="text-red-500">*</span></label>
          <p class="text-gray-600 text-sm mb-3">Brief summary that appears in the materials list</p>
          <textarea name="excerpt" rows="3" required
                    class="form-textarea w-full px-4 py-3 border-2 rounded-lg focus:outline-none resize-none">{{material.excerpt}}</textarea>
        </div>

        <!-- Content -->
        <div class="form-group mb-8">
          <label class="block text-gray-800 font-bold mb-3 text-lg">📖 Article Content <span class="text-red-500">*</span></label>
          <p class="text-gray-600 text-sm mb-3">Main content of the material</p>
          <textarea name="content" rows="15" required
                    class="form-textarea w-full px-4 py-3 border-2 rounded-lg focus:outline-none resize-none font-mono text-sm">{{material.content}}</textarea>
        </div>

        <!-- Reading Time -->
        <div class="form-group mb-8">
          <label class="block text-gray-800 font-bold mb-3 text-lg">⏱️ Estimated Reading Time <span class="text-red-500">*</span></label>
          <div class="flex items-center gap-4">
            <input type="number" name="readingTime" min="1" max="60" value="{{material.readingTime}}" required
                   class="form-input w-24 px-4 py-3 border-2 rounded-lg focus:outline-none">
            <span class="text-gray-600 font-semibold">minutes</span>
          </div>
        </div>

        <!-- Publish Status -->
        <div class="form-group mb-8">
          <label class="flex items-center cursor-pointer p-4 rounded-lg border-2 border-green-100 hover:bg-green-50 transition">
            <input type="checkbox" name="isPublished" value="true" {{#if material.isPublished}}checked{{/if}}
                   class="w-6 h-6 text-green-600 border-gray-300 rounded focus:ring-green-500 cursor-pointer">
            <div class="ml-4">
              <span class="text-gray-800 font-bold text-lg block">Published</span>
              <span class="text-gray-600 text-sm">Students can see this material</span>
            </div>
          </label>
        </div>

        <!-- Action Buttons -->
        <div class="form-group flex gap-4 pt-4 border-t-2 border-green-100">
          <button type="submit"
                  class="btn-primary flex-1 bg-green-600 text-white py-4 rounded-lg font-bold text-lg hover:bg-green-700 transition flex items-center justify-center gap-2">
            <span>✅</span> Update Material
          </button>
          <a href="/counselor/materials"
             class="flex-1 bg-gray-100 text-gray-700 py-4 rounded-lg font-bold text-lg hover:bg-gray-200 transition text-center flex items-center justify-center gap-2">
            <span>📋</span> All Materials
          </a>
          <button type="button" onclick="window.history.back()"
                  class="flex-1 bg-white border-2 border-gray-300 text-gray-700 py-4 rounded-lg font-bold text-lg hover:bg-gray-50 transition flex items-center justify-center gap-2">
            <span>←</span> Back
          </button>
        </div>
      </form>
    </div>

    <!-- Last Modified Info -->
    <div class="mt-6 bg-blue-50 border-2 border-blue-200 rounded-xl p-4 animate-fadeInUp">
      <p class="text-sm text-blue-800">
        <span class="font-bold">📝 Last Modified:</span> {{formatDate material.updatedAt}}
        {{#if material.views}}<span class="ml-4"><span class="font-bold">👁️ Views:</span> {{material.views}}</span>{{/if}}
      </p>
    </div>
  </div>

  <script>
    function handleFormSubmit(event) {
      event.preventDefault();
      
      const form = event.target;
      const title = form.querySelector('input[name="title"]').value.trim();
      const category = form.querySelector('select[name="category"]').value;
      const excerpt = form.querySelector('textarea[name="excerpt"]').value.trim();
      const content = form.querySelector('textarea[name="content"]').value.trim();
      const readingTime = form.querySelector('input[name="readingTime"]').value;
      
      // Validation
      if (!title) {
        alert('❌ Please enter a title');
        return;
      }
      if (!category) {
        alert('❌ Please select a category');
        return;
      }
      if (!excerpt) {
        alert('❌ Please provide a short description');
        return;
      }
      if (!content) {
        alert('❌ Please enter content');
        return;
      }
      if (readingTime < 1 || readingTime > 60) {
        alert('❌ Reading time must be between 1 and 60 minutes');
        return;
      }
      
      form.submit();
    }
  </script>
</body>
</html>
